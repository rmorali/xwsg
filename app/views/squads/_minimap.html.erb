<% @planets.each do |planet| %>
  <%= image_tag (planet.image), style: "width: 10px; position: absolute; left:#{ planet.x * 0.20 + adjust_x(planet.sector) }%; top:#{ planet.y * 0.20 + adjust_y(planet.sector) }%" %>
<% end %>
